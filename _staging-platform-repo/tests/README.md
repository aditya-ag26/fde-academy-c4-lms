# Platform tests · PLATFORM REPO

Tests for `lmskit`.

The validator is the piece most worth testing: it is the thing standing between a typo
in frontmatter and a silently broken dashboard, and it runs as a required check on
every pull request in every batch.

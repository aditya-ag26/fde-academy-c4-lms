# `lmskit/` · PLATFORM REPO

The shared Python package: frontmatter parsing and validation, dashboard and
calendar generation, the Discussions bot library, and the GitHub API client.

Installed by the content repo's CI through a reusable workflow. Never vendored into the
content repo — that is the drift this separation exists to prevent.

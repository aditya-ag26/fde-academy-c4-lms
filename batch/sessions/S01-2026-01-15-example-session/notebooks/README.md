# `notebooks/` — S01

Executable notebooks. This example session has none (`notebooks: absent`).

Real notebooks are executed in CI and must run clean from a fresh kernel — a notebook that only works in the author's environment is a support ticket waiting to happen.

# `assets/` — S01

Images and diagrams referenced by this session's documents.

Keep them alongside the session rather than in a shared folder — a session folder should be movable and archivable as a unit.

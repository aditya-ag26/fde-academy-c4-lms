# `code/` — S01

Code samples used in the session. Small, runnable, and referenced from the session README or post-read.

Anything a student needs to run themselves belongs in the activity brief instead, so it has a rubric and a submission path.

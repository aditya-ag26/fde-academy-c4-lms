# `generate/` · PRIVATE REPO

Produce drafts from ingested sources using a versioned skill.

Records `skill`, `skill_version`, `model` and the run URL into the draft's provenance
block. A draft with no traceable generator does not proceed.

# `validate/` · PRIVATE REPO

Check a draft against the frontmatter schema **before a human sees it**.

Sending a reviewer a draft with invalid frontmatter wastes the expensive part of the
loop. Machine-checkable failures are caught by the machine.
